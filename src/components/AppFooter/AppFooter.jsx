/**
 * @jsx React.DOM
 */

var React = require('react')

require('components/AppFooter/AppFooter.scss')

var AppFooter = React.createClass({

  render() {
    return (
      <div className="AppFooter" >
      </div>
    )
  }

})

module.exports = AppFooter
